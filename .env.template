# ============================================================================
# AIOS Environment Variables Template
# ============================================================================
# OPTION 1: Copy to .env (loaded by dotenv)
# OPTION 2: Set as system environment variables (recommended)
#
# PowerShell (permanent):
#   [System.Environment]::SetEnvironmentVariable('GEMINI_API_KEY', 'your-key', 'User')
#
# PowerShell (session):
#   $env:GEMINI_API_KEY = 'your-key'
# ============================================================================

# Google Gemini API Key (get from https://aistudio.google.com/app/apikey)
GEMINI_API_KEY=

# OpenRouter API Key (get from https://openrouter.ai/keys)
OPENROUTER_API_KEY=

# Ollama (local - no key needed, just host)
OLLAMA_HOST=http://localhost:11434

# AIOS Ports (optional overrides)
# AIOS_CYTOPLASM_PORT=9002
# AIOS_INTERFACE_BRIDGE_PORT=8000
